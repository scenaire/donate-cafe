// Ambient declarations for the app.

// Side-effect (global) CSS imports, e.g. `import "./styles.css";`.
// Next.js handles these through its webpack loader at build time; this
// declaration is only so TypeScript stops reporting TS2882.
declare module "*.css";
